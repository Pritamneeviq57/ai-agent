[build]
builder = "nixpacks"

[deploy]
# Run Flask API on Railway
startCommand = "gunicorn app:app --bind 0.0.0.0:$PORT --timeout 600 --graceful-timeout 600 --workers 2"
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3

